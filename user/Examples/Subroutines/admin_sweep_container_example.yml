name: Admin Sweep Container (Example)
type: subroutine
category: Work
frequency: weekly
duration: 50
tags:
  - example
  - pilot_admin
children:
  - type: task
    name: Admin Sweep 30 (Example)
  - type: task
    name: Weekly Money Check 20 (Example)
notes: Batches low-intensity admin so it doesn't leak into focus windows.

