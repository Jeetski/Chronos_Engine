name: Pilot Takeoff Routine (Example)
type: routine
category: Work
frequency: weekdays
duration: 150
status_requirements:
  energy:
    - medium
    - high
  focus:
    - good
    - laser
children:
  - type: microroutine
    name: Cockpit Boot Stack (Example)
  - type: subroutine
    name: Focus Block Container (Example)
variants:
  - name: Pilot Takeoff Recovery Variant
    status_requirements:
      energy:
        - low
        - exhausted
    duration: 90
    children:
      - type: microroutine
        name: Cockpit Boot Stack (Example)
      - type: task
        name: Power Nap Reset (Example)
      - type: habit
        name: Sunlight Walk 10 (Example)
notes: Primary launch routine with a built-in low-energy fallback.

