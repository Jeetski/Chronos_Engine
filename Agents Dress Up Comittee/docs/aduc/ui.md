# UI Flow

The UI is a minimal single-page app.

Flow
1) Load familiars and populate the dropdown.
2) Send user input to `/chat`.
3) Render reply and update avatar based on the tag.

Files
- `ADUC.html`: base layout.
- `static/ui.js`: client logic.
- `static/styles.css`: styling.
