ARCHIVE_PATH = r"C:\Users\david\Desktop\Hivemind Studio\Chronos Engine\META\LEGACY_SCHEDULING\modules\scheduler\weekly_generator.py"


class WeeklyGenerator:
    def __init__(self, *args, **kwargs):
        raise RuntimeError(
            f"Legacy weekly skeleton generation has been archived to {ARCHIVE_PATH}. "
            "A Kairos v2-native weekly planner has not been implemented yet."
        )


def save_weekly_skeleton(path, payload):
    raise RuntimeError(
        f"Legacy weekly skeleton generation has been archived to {ARCHIVE_PATH}. "
        "A Kairos v2-native weekly planner has not been implemented yet."
    )
